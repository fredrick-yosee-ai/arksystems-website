<div class="value-strip">
    <div class="value-item">
        <svg viewBox="0 0 48 48" aria-hidden="true">
            <circle cx="20" cy="20" r="12" />
            <path d="m29 29 12 12" />
        </svg>

        <div>
            <h3>Find the leaks</h3>
            <p>See what’s really costing you time and money.</p>
        </div>
    </div>

    <div class="value-item">
        <svg viewBox="0 0 48 48" aria-hidden="true">
            <path d="M7 40h34" />
            <path d="M11 40V30h6v10" />
            <path d="M21 40V22h6v18" />
            <path d="M31 40V10h6v30" />
        </svg>

        <div>
            <h3>Prove the value</h3>
            <p>Quantify the impact before you invest.</p>
        </div>
    </div>

    <div class="value-item">
        <svg viewBox="0 0 48 48" aria-hidden="true">
            <path d="M24 7v34" />
            <path d="M13 11h22" />
            <path d="m13 11-7 15h14z" />
            <path d="m35 11-7 15h14z" />
            <path d="M16 41h16" />
        </svg>

        <div>
            <h3>Compare the investment</h3>
            <p>Right-size the solution for real outcomes.</p>
        </div>
    </div>

    <div class="value-item">
        <svg viewBox="0 0 48 48" aria-hidden="true">
            <circle cx="24" cy="24" r="18" />
            <path d="m15 24 6 6 13-14" />
        </svg>

        <div>
            <h3>Proceed with confidence</h3>
            <p>Implement only what makes business sense.</p>
        </div>
    </div>
</div>

<style>
    .value-strip {
        display: grid;
        grid-template-columns: repeat(4, minmax(0, 1fr));
        border: 1px solid rgb(79 84 64 / 8%);
        border-radius: 1rem;
        background: rgb(232 232 219 / 55%);
        overflow: hidden;
    }

    .value-item {
        display: grid;
        grid-template-columns: 3rem minmax(0, 1fr);
        align-items: center;
        gap: 1.25rem;
        min-width: 0;
        padding: 1.5rem 1.8rem;
        border-right: 1px solid rgb(79 84 64 / 18%);
    }

    .value-item:last-child {
        border-right: 0;
    }

    .value-item svg {
        width: 3rem;
        height: 3rem;
        fill: none;
        stroke: var(--color-brand-700);
        stroke-width: 1.8;
        stroke-linecap: round;
        stroke-linejoin: round;
    }

    .value-item h3 {
        margin: 0 0 0.4rem;
        font-family: var(--font-body);
        font-size: 1rem;
        font-weight: 600;
        line-height: 1.25;
        color: var(--color-brand-900);
    }

    .value-item p {
        margin: 0;
        font-size: 0.86rem;
        line-height: 1.45;
        color: var(--color-neutral-700);
    }

    @media (max-width: 68rem) {
        .value-strip {
            grid-template-columns: repeat(2, minmax(0, 1fr));
        }

        .value-item:nth-child(2) {
            border-right: 0;
        }

        .value-item:nth-child(-n + 2) {
            border-bottom: 1px solid rgb(79 84 64 / 18%);
        }
    }

    @media (max-width: 40rem) {
        .value-strip {
            grid-template-columns: 1fr;
        }

        .value-item {
            grid-template-columns: 2.5rem minmax(0, 1fr);
            gap: 1rem;
            padding: 1.25rem 1rem;
            border-right: 0;
            border-bottom: 1px solid rgb(79 84 64 / 18%);
        }

        .value-item:nth-child(2) {
            border-bottom: 1px solid rgb(79 84 64 / 18%);
        }

        .value-item:last-child {
            border-bottom: 0;
        }

        .value-item svg {
            width: 2.5rem;
            height: 2.5rem;
        }

        .value-item h3 {
            font-size: 0.95rem;
        }

        .value-item p {
            font-size: 0.8rem;
        }
    }
</style>