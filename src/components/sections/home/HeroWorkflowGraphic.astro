<div class="workflow-visual">
    <div class="workflow-visual__arc" aria-hidden="true"></div>
    <div class="workflow-visual__dots" aria-hidden="true"></div>

    <picture class="workflow-visual__picture">
        <source
                media="(max-width: 62rem)"
                srcset="/hero_banner_mobile.svg"
        />

        <img
                src="/hero_banner_desktop.svg"
                alt="Workflow showing information moving from business systems through collection, review and completion."
                class="workflow-visual__image"
        />
    </picture>

    <div class="workflow-summary">
        <div class="workflow-summary__main">
            <p class="workflow-summary__heading">Workflow at a glance</p>

            <div class="workflow-summary__metrics">
                <div class="workflow-metric">
					<span class="workflow-metric__icon workflow-metric__icon--waiting material-symbols-rounded">
						schedule
					</span>

                    <div class="workflow-metric__content">
                        <strong>12</strong>
                        <span>Waiting</span>
                    </div>
                </div>

                <div class="workflow-metric">
					<span class="workflow-metric__icon workflow-metric__icon--blocked material-symbols-rounded">
						priority_high
					</span>

                    <div class="workflow-metric__content">
                        <strong>4</strong>
                        <span>Blocked</span>
                    </div>
                </div>

                <div class="workflow-metric">
					<span class="workflow-metric__icon workflow-metric__icon--ready material-symbols-rounded">
						check
					</span>

                    <div class="workflow-metric__content">
                        <strong>3</strong>
                        <span>Ready</span>
                    </div>
                </div>
            </div>
        </div>

        <div class="workflow-summary__meaning">
            <p class="workflow-summary__heading">What this means</p>
            <p>Work is getting stuck before it’s complete.</p>
            <strong>We help you fix that.</strong>
        </div>
    </div>
</div>

<style>
    .workflow-visual {
        position: relative;
        isolation: isolate;
        width: 100%;
    }

    .workflow-visual__picture {
        position: relative;
        z-index: 2;
        display: block;
        width: 100%;
    }

    .workflow-visual__image {
        display: block;
        width: 100%;
        height: auto;
    }

    .workflow-visual__arc,
    .workflow-visual__dots {
        position: absolute;
        z-index: 1;
        pointer-events: none;
    }

    .workflow-visual__arc {
        top: -5%;
        left: 16%;
        width: 72%;
        aspect-ratio: 1;
        border: 2.4rem solid rgb(158 159 126 / 7%);
        border-radius: 50%;
    }

    .workflow-visual__arc::after {
        content: "";
        position: absolute;
        inset: 12%;
        border: 1.8rem solid rgb(158 159 126 / 5%);
        border-radius: 50%;
    }

    .workflow-visual__dots {
        right: -1rem;
        bottom: 4rem;
        width: 8rem;
        height: 8rem;
        background-image:
                radial-gradient(
                        circle,
                        rgb(201 138 24 / 26%) 1.4px,
                        transparent 1.4px
                );
        background-size: 0.95rem 0.95rem;
        opacity: 0.48;
    }

    .workflow-summary {
        position: relative;
        z-index: 3;
        display: grid;
        grid-template-columns: minmax(0, 1fr) 14rem;
        gap: 1.5rem;
        margin: -0.65rem 1.75rem 0;
        padding: 1.4rem 1.6rem;
        border: 1px solid rgb(79 84 64 / 11%);
        border-radius: 0.9rem;
        background: rgb(255 253 248 / 94%);
        box-shadow: 0 12px 28px rgb(58 63 46 / 8%);
        backdrop-filter: blur(3px);
    }

    .workflow-summary__heading {
        margin: 0 0 1rem;
        font-size: 0.86rem;
        font-weight: var(--font-weight-semibold);
        color: var(--color-neutral-700);
    }

    .workflow-summary__metrics {
        display: grid;
        grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    .workflow-metric {
        display: flex;
        align-items: center;
        gap: 0.9rem;
        min-width: 0;
        padding-inline: 1.15rem;
        border-right: 1px solid var(--color-border);
    }

    .workflow-metric:first-child {
        padding-left: 0;
    }

    .workflow-metric:last-child {
        border-right: 0;
    }

    .workflow-metric__icon {
        display: grid;
        flex: 0 0 2.45rem;
        width: 2.45rem;
        height: 2.45rem;
        place-items: center;
        border-radius: 50%;
        font-size: 1.4rem;
    }

    .workflow-metric__icon--waiting {
        background: #efe4be;
        color: var(--color-brand-700);
    }

    .workflow-metric__icon--blocked {
        background: #c98a18;
        color: var(--color-white);
    }

    .workflow-metric__icon--ready {
        background: var(--color-brand-400);
        color: var(--color-white);
    }

    .workflow-metric__content {
        display: flex;
        min-width: 0;
        flex-direction: column;
    }

    .workflow-metric__content strong {
        font-size: 1.75rem;
        line-height: 1;
        color: var(--color-neutral-900);
    }

    .workflow-metric__content span {
        margin-top: 0.3rem;
        font-size: 0.74rem;
        color: var(--color-text-muted);
    }

    .workflow-summary__meaning {
        padding-left: 1.5rem;
        border-left: 1px solid var(--color-border);
    }

    .workflow-summary__meaning p:not(.workflow-summary__heading) {
        margin: 0;
        font-size: 0.83rem;
        line-height: 1.5;
        color: var(--color-neutral-700);
    }

    .workflow-summary__meaning strong {
        display: block;
        margin-top: 0.4rem;
        font-size: 0.83rem;
        color: var(--color-brand-700);
    }

    @media (max-width: 75rem) {
        .workflow-summary {
            grid-template-columns: minmax(0, 1fr) 12rem;
            margin-inline: 1rem;
            padding-inline: 1.25rem;
        }
    }

    @media (max-width: 62rem) {
        .workflow-visual {
            max-width: 42rem;
            margin-inline: auto;
        }

        .workflow-visual__arc {
            top: 4%;
            left: 7%;
            width: 86%;
        }

        .workflow-summary {
            grid-template-columns: 1fr;
            gap: 1.25rem;
            margin: 0.75rem 0 0;
        }

        .workflow-summary__meaning {
            padding-top: 1.25rem;
            padding-left: 0;
            border-top: 1px solid var(--color-border);
            border-left: 0;
        }
    }

    @media (max-width: 40rem) {
        .workflow-visual__arc {
            border-width: 1.4rem;
        }

        .workflow-visual__arc::after {
            border-width: 0.9rem;
        }

        .workflow-visual__dots {
            right: -0.5rem;
            bottom: 3rem;
            width: 5rem;
            height: 5rem;
            background-size: 0.7rem 0.7rem;
        }

        .workflow-summary {
            padding: 1.15rem;
            border-radius: 0.8rem;
        }

        .workflow-summary__metrics {
            gap: 0.75rem;
        }

        .workflow-metric {
            flex-direction: column;
            align-items: flex-start;
            gap: 0.45rem;
            padding-inline: 0.75rem;
        }

        .workflow-metric:first-child {
            padding-left: 0;
        }

        .workflow-metric__icon {
            width: 2rem;
            height: 2rem;
            flex-basis: 2rem;
            font-size: 1.15rem;
        }

        .workflow-metric__content strong {
            font-size: 1.35rem;
        }
    }
</style>